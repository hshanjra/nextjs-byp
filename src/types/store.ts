import { CartSlice } from "@/store/cart-slice";
import { UserSlice } from "@/store/user-slice";
import { VehicleSlice } from "@/store/vehicle-store";

// export type Store = UserSlice & CartSlice;
export type Store = VehicleSlice;
